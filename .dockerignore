# Both Dockerfiles copy only dist/apps/<app>/ (plus .npmrc for connect), so
# everything below is dead weight in the build context.
node_modules
**/node_modules
.git
.nx
tmp
logs
.data
play
bruno
docs
apps
libs
prisma/migrations
.env
.env.*
!.env.example
!.env.worker.example
