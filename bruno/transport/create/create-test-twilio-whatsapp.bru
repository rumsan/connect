meta {
  name: create-test-twilio-whatsapp
  type: http
  seq: 7
}

post {
  url: {{url}}/transports
  body: json
  auth: none
}

headers {
  app-id: {{app-id}}
}

body:json {
  {
      "app": "{{app-id}}",
      "name": "Whatsapp Test Messaging",
      "type": "API",
      "config": {
          "url": "{{TWILIO_URL}}",
          "body": {
              "From": "{{TWILIO_WHATSAPP_FROM}}",
              "To": "{%address%}",
              "ContentSid": "{%message.content%}"
          },
   "headers": {
          "Content-Type": "application/x-www-form-urlencoded",
          "Authorization": "{{TWILIO_WHATSAPP_AUTH}}"
      },
            "meta": {
          "provider": "twilio",
          "apiSecret": "{{TWILIO_API_SECRET}}",
          "accountSid": "{{TWILIO_ACCOUNT_SID}}",
          "capabilities": [
              "TEMPLATE_VERIFICATION"
          ]
      }
      }
  }
}

tests {
  var data = res.getBody().data;
  
  if(data){
    bru.setVar("transport-id-api", data.cuid);
    bru.setVar("transport-id", data.cuid);
  }
}
