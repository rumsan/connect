meta {
  name: list-by-app
  type: http
  seq: 1
}

get {
  url: {{url}}/broadcasts
  body: json
  auth: none
}

headers {
  app-id: {{app-id}}
}

tests {
  var data = res.getBody().data;
  bru.setVar("broadcast-id", data[0].cuid);
}
